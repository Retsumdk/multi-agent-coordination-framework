{
  "name": "multi-agent-coordination-framework",
  "description": "Framework for coordinating multiple autonomous agents with shared state, conflict resolution, and collaborative task distribution",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "build": "tsc"
  },
  "dependencies": {
    "commander": "^11.1.0"
  },
  "devDependencies": {
    "bun-types": "^1.0.0",
    "typescript": "^5.3.0"
  }
}